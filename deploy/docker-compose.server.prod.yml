services:
  server:
    image: ${REGISTRY_URL}/${REPOSITORY_NAME}/server:${GITHUB_SHA}
    container_name: server-prod
    restart: unless-stopped
    env_file:
      - .env.prod
    environment:
      - NODE_ENV=production
    ports:
      - "3000:3000"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3000/api/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
